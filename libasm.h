#ifndef LIBASM_H
# define LIBASM_H

#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <stdlib.h>
#include <errno.h>

extern int	ft_strlen(char *s);
extern char	*ft_strcpy(char *dst, const char *src);

#endif